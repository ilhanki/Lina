# Scripts

Utility scripts for development and operations live here.

Examples include setup helpers, local diagnostics, model management scripts, database migrations, and maintenance tasks. Scripts should call into `src/lina` code when possible instead of duplicating business logic.

